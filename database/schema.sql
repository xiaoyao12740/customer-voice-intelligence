CREATE DATABASE IF NOT EXISTS voc_platform CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE voc_platform;
-- SQLAlchemy creates reviews, predictions, model_versions and batch_jobs on application startup.

